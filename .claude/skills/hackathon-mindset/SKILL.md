---
name: hackathon-mindset
description: The standing priority order for this hackathon build - solve the use case with AI over writing good code, protect the demo path over adding features, cut scope not reliability. Invoke at the start of the day and whenever a scope/quality tradeoff needs the full reasoning, instead of re-reading multiple files.
---

Read and apply `MINDSET.md` at the repo root — that file is the single canonical source
for this doctrine (kept there, not duplicated here, so there's one place to update and
no risk of the two drifting apart). This skill exists so the reasoning can be pulled into
context on demand rather than sitting fully loaded in every session by default.
